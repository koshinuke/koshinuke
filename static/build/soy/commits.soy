{namespace org.koshinuke.template.commits}

/**
 * @param author
 * @param message
 * @param timestamp
 */
{template .tmpl}
<div class="commit">
	<img class="author thumb large {$author}"/>
	<div class="metadata">
		<div class="message">{$message}</div>
		<div class="commitdata">
			<span class="name">{$author}</span>authored
			<span class="timestamp">{$timestamp}</span>
		</div>
	</div>
</div>
{/template}
